import React from 'react';

const DeleteBill = () => {

    return (
        <div>
            <h2>You are going to delete this bill. If you over sure to delete it then Confirm delete.</h2>

            {/* <button onClick={confirmDelete}>Confirm delete</button> */}
        </div>
    );
};

export default DeleteBill;